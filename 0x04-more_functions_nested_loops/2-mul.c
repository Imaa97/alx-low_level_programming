#include "holberton.h"
/**
 *mul(int a, int b)- multplies two numbers
 *@a:a character in ascii
 *@b:a character in ascii
 *Return: result of the calculation
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}

