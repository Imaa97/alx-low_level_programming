#ifndef ABS
#define ABS absolute_value
/**
 *ABS - function that findds value of absolute value
 *@x: number to evaluated
 *Return: The computation within the parenthesis.
 */
int ABS(int x)
{
	return ((x < 0) ? (-x) : (x));
}
#endif
